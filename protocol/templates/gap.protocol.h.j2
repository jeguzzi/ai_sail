{% from "macros.j2" import typedef, cb, check, param  -%}
#include <stdint.h>

{%- for data_t in config.configs %}
{{ typedef(data_t) }}
#define {{data_t.name | upper}}_HEADER "{{data_t.header_value}}"
void send_{{data_t.name}}_config({{data_t.typename}} *config);
// To be implemented if {{data_t.name | upper}}_HEADER is defined
void set_{{data_t.name}}_config({{data_t.typename}} *config);
{% endfor -%}

{%- for data_t in config.inputs %}
{{ typedef(data_t) }}
#define {{data_t.name | upper}}_HEADER "{{data_t.header_value}}"
void send_{{data_t.name}}({{data_t.typename}} *config);
{% endfor -%}
